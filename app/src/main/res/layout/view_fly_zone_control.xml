<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout_fly_zone_control"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <androidx.appcompat.widget.LinearLayoutCompat
        android:id="@+id/layout_linear_fly_zone_control"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:orientation="horizontal">

        <Button
            android:id="@+id/geo_unlock_nfzs_btn"
            style="@style/LeftButtonList"
            android:layout_weight="1"
            android:text="@string/geo_unlock_nfzs"/>

        <Button
            android:id="@+id/geo_get_unlock_nfzs_btn"
            style="@style/LeftButtonList"
            android:text="@string/geo_get_unlock_nfzs"
            android:layout_weight="1" />

        <Button
            android:id="@+id/geo_get_surrounding_nfz_btn"
            style="@style/LeftButtonList"
            android:layout_weight="1"
            android:text="@string/geo_get_surrounding_nfz" />

    </androidx.appcompat.widget.LinearLayoutCompat>

    <androidx.core.widget.NestedScrollView
        android:layout_width="300dp"
        android:layout_height="300dp"
        app:layout_constraintTop_toBottomOf="@id/layout_linear_fly_zone_control"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginStart="64dp"
        android:layout_marginTop="10dp"
        >
        <TextView
            android:id="@+id/txt_unlock_status"
            android:layout_width="300dp"
            android:layout_height="wrap_content"
            android:background="@color/black_alpha"
            android:textColor="@color/white"
            />
    </androidx.core.widget.NestedScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>